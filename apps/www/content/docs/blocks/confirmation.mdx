---
title: Confirmation Dialog
description: The Confirmation Dialog component provides a consistent way to ask users for confirmation before performing destructive or important actions
---
import { TypeTable } from 'fumadocs-ui/components/type-table';

## Preview
<ComponentPreview name="confirmation-preview" />

## Installation
```package-install
npx shadcn add @uilab-core/confirmation
```

## Usage
Wrap your app with the provider
```tsx
import { ConfirmationProvider } from '@/components/confirmation';

function App() {
  return (
    <ConfirmationProvider>
      <MyApp />
    </ConfirmationProvider>
  );
}
```
```tsx
import { useConfirmation } from '@/components/confirmation';
```
```tsx
showDialog({
  title: 'Delete Item',
  description: 'Are you sure you want to delete this item?',
  variant: 'destructive'
  confirmText: 'Delete',
  cancelText: 'Cancel',
  onConfirm: () => {
    // Handle confirmation
  },
})
```

## API References
### ConfirmationOptions
<TypeTable
  type={{
    title: {
      type: <code>string</code>,
      description: "The title of the confirmation dialog",
    },
    description: {
      type: <code>string</code>,
      description: "The description or message shown in the dialog",
    },
    variant: {
      type: <code>destructive | default</code>,
      default: <code>default</code>,
      description: "Variant of confirmation dialog"
    },
    confirmText: {
      type: <code>string</code>,
      default: "Confirm",
      description: "Text for the confirm button",
    },
    cancelText: {
      type: <code>string</code>,
      default: "Cancel",
      description: "Text for the cancel button",
    },
    onConfirm: {
      type: <code>{"() => void"}</code>,
      description: "Callback when user confirms",
    },
    onCancel: {
      type: <code>{"() => void"}</code>,
      description: "Callback when user cancels",
    },
  }}
/>
