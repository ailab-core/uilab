import type { DemoItem } from '..'

import { BreadcrumbPreview } from "./preview"

export const BreadcrumbDemos: Record<string, DemoItem> = {
  "breadcrumb-preview": {
    component: BreadcrumbPreview,
    file: 'breadcrumb/preview.tsx',
  },
}
