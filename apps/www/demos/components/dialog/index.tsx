import type { DemoItem } from '@/demos'

import { DialogPreview } from "./preview"

export const DialogDemos: Record<string, DemoItem> = {
  "dialog-preview": {
    component: DialogPreview,
    file: "components/dialog/preview.tsx",
  },
}
