import { type DemoItem } from '..'

import { CommandPreview } from './preview'

export const CommandDemos: Record<string, DemoItem> = {
  "command-preview": {
    component: CommandPreview,
    file: 'command/preview.tsx',
  },
}

