import type { DemoItem } from '..'

import { AvatarExample } from "./preview"

export const AvatarDemos: Record<string, DemoItem> = {
  "avatar-preview": {
    component: AvatarExample,
    file: 'avatar/preview.tsx',
  },
}
