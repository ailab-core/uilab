import type { DemoItem } from ".."

import { LabelPreview } from "./preview"

export const LabelDemos: Record<string, DemoItem> = {
  "label-preview": {
    component: LabelPreview,
    file: "label/preview.tsx",
  },
}
