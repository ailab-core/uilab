import type { DemoItem } from "@/demos"
import { DataGridPreview } from "./preview"

export const DataGridDemos: Record<string, DemoItem> = {
  "data-grid-preview": {
    component: DataGridPreview,
    file: "blocks/data-grid/preview.tsx",
  },
}
