import type { DemoItem } from "@/demos"

import { ConfirmationPreview } from "./preview"

export const ConfirmationDemos: Record<string, DemoItem> = {
  "confirmation-preview": {
    component: ConfirmationPreview,
    file: "blocks/confirmation/preview.tsx",
  }
}
