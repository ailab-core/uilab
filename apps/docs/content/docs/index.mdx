---
title: Get started
description: Getting started with uilab-core component library
---

import {
  PackageIcon,
  PaletteIcon,
  CircleCheckIcon,
  CircleMinusIcon,
  CodeIcon,
  ComponentIcon,
  FolderGit2Icon,
  ZapIcon,
} from 'lucide-react'

## Introduction

**uilab-core** is a comprehensive React component library built with modern web technologies for AI Lab company's internal use. It provides 50+ production-ready, accessible UI components designed for building beautiful and consistent user interfaces.

### Key Features

<Cards>
  <Card
    icon={<PackageIcon />}
    title='50+ Components'
    description='Comprehensive collection of UI components for all your needs'
  />
  <Card
    icon={<PaletteIcon />}
    title='Tailwind CSS'
    description='Styled with Tailwind CSS v4 for easy customization'
  />
  <Card
    icon={<CodeIcon />}
    title='TypeScript First'
    description='Fully typed with TypeScript for the best developer experience'
  />
  <Card
    icon={<ZapIcon />}
    title='Radix UI'
    description='Built on Radix UI primitives for accessibility and flexibility'
  />
</Cards>

## Installation

### Using npm

```bash
npm install uilab-core tw-animate-css lucide-react
```

## Setup

### 1. Import Styles

Import the component styles in your application entry point:

```css title='app/global.css'
@import 'tailwindcss';
@import 'tw-animate-css';
@import 'uilab-core/styles.css';
@import 'uilab-core/theme.css';

/* Make sure to add this too for themes */
@custom-variant dark (&:is(.dark *));
```

## Basic Usage

Import and use components in your React application:

```tsx
import { Button, Card, CardHeader, CardTitle, CardContent } from 'uilab-core'

export function Example() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Welcome to uilab-core</CardTitle>
      </CardHeader>
      <CardContent>
        <Button>Get Started</Button>
      </CardContent>
    </Card>
  )
}
```

## Next Steps

<Cards>
  <Card
    title='Browse Components'
    description='Explore all available components with live examples'
    href='/docs/ui/components'
    icon={<ComponentIcon />}
  />
  <Card
    title='View Examples'
    description='See components in action with project examples'
    href='/docs/examples'
    icon={<FolderGit2Icon />}
  />
</Cards>

## TypeScript Support

All components are written in TypeScript and include complete type definitions. You'll get full IntelliSense support in your IDE:

```tsx
import { Button, type ButtonProps } from 'uilab-core'

// Full type support for props
const MyButton: React.FC<ButtonProps> = (props) => {
  return <Button {...props} />
}
```

## Accessibility

All components are built with accessibility in mind, following WAI-ARIA guidelines:

- Keyboard navigation support
- Screen reader friendly
- Focus management
- ARIA attributes

## Framework Compatibility

uilab-core works with:

- Next.js 13+ (App Router and Pages Router) - *<CircleCheckIcon className='inline-block size-4 text-emerald-600 dark:text-emerald-300' /> Tested*
- Vite + React - *<CircleCheckIcon className='inline-block size-4 text-emerald-600 dark:text-emerald-300' /> Tested*
- Remix - *<CircleMinusIcon className='inline-block size-4 text-red-600 dark:text-red-300' /> Untested*

For Next.js 13+ with App Router, make sure to add the `transpilePackages` configuration as shown in the setup section above.
