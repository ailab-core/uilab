---
title: Context Menu
description: A menu that appears on right-click.
---

import { ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem, ContextMenuCheckboxItem, ContextMenuRadioGroup, ContextMenuRadioItem, ContextMenuLabel, ContextMenuSeparator, ContextMenuShortcut } from "uilab-core"

The Context Menu component displays a menu when right-clicking on an element.

## Examples

### Default Context Menu

<ComponentPreview title="Default Context Menu">
  <ContextMenu>
    <ContextMenuTrigger className="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
      Right click here
    </ContextMenuTrigger>
    <ContextMenuContent className="w-64">
      <ContextMenuItem>Back</ContextMenuItem>
      <ContextMenuItem>Forward</ContextMenuItem>
      <ContextMenuItem>Reload</ContextMenuItem>
      <ContextMenuSeparator />
      <ContextMenuItem>Save Page As...</ContextMenuItem>
      <ContextMenuItem>Print...</ContextMenuItem>
    </ContextMenuContent>
  </ContextMenu>
</ComponentPreview>

```tsx
import {
  ContextMenu,
  ContextMenuTrigger,
  ContextMenuContent,
  ContextMenuItem
} from 'uilab-core'

export function Example() {
  return (
    <ContextMenu>
      <ContextMenuTrigger>Right click here</ContextMenuTrigger>
      <ContextMenuContent>
        <ContextMenuItem>Back</ContextMenuItem>
        <ContextMenuItem>Forward</ContextMenuItem>
        <ContextMenuItem>Reload</ContextMenuItem>
      </ContextMenuContent>
    </ContextMenu>
  )
}
```

### With Checkboxes

<ComponentPreview title="Context Menu with Checkboxes">
  <ContextMenu>
    <ContextMenuTrigger className="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
      Right click here
    </ContextMenuTrigger>
    <ContextMenuContent>
      <ContextMenuCheckboxItem checked>Show Toolbar</ContextMenuCheckboxItem>
      <ContextMenuCheckboxItem>Show Sidebar</ContextMenuCheckboxItem>
      <ContextMenuCheckboxItem checked>Show Footer</ContextMenuCheckboxItem>
    </ContextMenuContent>
  </ContextMenu>
</ComponentPreview>

```tsx
<ContextMenuCheckboxItem checked>
  Show Toolbar
</ContextMenuCheckboxItem>
```

## Components

Context Menu uses the same components as Dropdown Menu:

- **ContextMenu**: Root component
- **ContextMenuTrigger**: Element that triggers the menu
- **ContextMenuContent**: Menu content container
- **ContextMenuItem**: Individual menu item
- **ContextMenuCheckboxItem**: Checkable menu item
- **ContextMenuRadioGroup**: Radio group container
- **ContextMenuRadioItem**: Radio menu item
- **ContextMenuLabel**: Label for menu sections
- **ContextMenuSeparator**: Visual separator
- **ContextMenuShortcut**: Keyboard shortcut display

The API is similar to Dropdown Menu. See Dropdown Menu documentation for detailed props.
