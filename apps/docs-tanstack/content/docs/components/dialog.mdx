---
title: Dialog
description: A modal dialog that overlays the page content.
---

import {
  Button,
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "uilab-core"

The Dialog component displays content in a modal overlay.

## Examples

### Default Dialog

<ComponentPreview title="Default Dialog">
  <Dialog>
    <DialogTrigger asChild>
      <Button variant="outline">Open Dialog</Button>
    </DialogTrigger>
    <DialogContent>
      <DialogHeader>
        <DialogTitle>Dialog Title</DialogTitle>
        <DialogDescription>
          This is a description of the dialog content.
        </DialogDescription>
      </DialogHeader>
      <div className="py-4">
        <p>Dialog content goes here.</p>
      </div>
      <DialogFooter>
        <Button variant="outline">Cancel</Button>
        <Button>Confirm</Button>
      </DialogFooter>
    </DialogContent>
  </Dialog>
</ComponentPreview>

```tsx
import {
  Button,
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "uilab-core"

export function Example() {
  return (
    <Dialog>
      <DialogTrigger asChild>
        <Button variant="outline">Open Dialog</Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Dialog Title</DialogTitle>
          <DialogDescription>
            This is a description of the dialog content.
          </DialogDescription>
        </DialogHeader>
        <div className="py-4">
          <p>Dialog content goes here.</p>
        </div>
        <DialogFooter>
          <Button variant="outline">Cancel</Button>
          <Button>Confirm</Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

### Without Close Button

<ComponentPreview title="Dialog without Close Button">
  <Dialog>
    <DialogTrigger asChild>
      <Button variant="outline">Open</Button>
    </DialogTrigger>
    <DialogContent showCloseButton={false}>
      <DialogHeader>
        <DialogTitle>Are you sure?</DialogTitle>
        <DialogDescription>
          This action requires confirmation.
        </DialogDescription>
      </DialogHeader>
      <DialogFooter>
        <Button variant="outline">No, cancel</Button>
        <Button>Yes, continue</Button>
      </DialogFooter>
    </DialogContent>
  </Dialog>
</ComponentPreview>

```tsx
<DialogContent showCloseButton={false}>{/* Content */}</DialogContent>
```

## Components

- **Dialog**: The root dialog component
- **DialogTrigger**: The button that opens the dialog
- **DialogContent**: The dialog content container
- **DialogHeader**: Container for title and description
- **DialogFooter**: Container for action buttons
- **DialogTitle**: The dialog title
- **DialogDescription**: The dialog description

## Props

### Dialog

- `open`: `boolean` - The controlled open state
- `defaultOpen`: `boolean` - The default open state
- `onOpenChange`: `(open: boolean) => void` - Open state change handler
- `modal`: `boolean` - Whether the dialog is modal (default: true)

### DialogContent

- `showCloseButton`: `boolean` - Whether to show the close button (default: true)
